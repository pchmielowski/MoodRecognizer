#pragma once
#include <opencv\cv.h>
#include <string>
#include <vector>

typedef std::string			FileName;
typedef cv::Mat				FeatureMatrix; 
typedef float				Alpha;
typedef cv::Mat				SuperVector;
typedef int					Mood;